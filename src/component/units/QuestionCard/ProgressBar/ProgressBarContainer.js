import React from "react";
import ProgressBarPresenter from "./ProgressBarPresenter";

const ProgressBarContainer = () => {
  return <ProgressBarPresenter progress={70} />;
};

export default ProgressBarContainer;
